# Dependencies and build output
node_modules/
dist/

# Runtime state: the database (Postgres in production, SQLite locally), the
# documents the console generates, and the per-mission working copies.
data/
workspaces/

# Secrets
.env
.env.local

# Claude Code's own scratch directory at the repository root.
# NOT product/.claude/ - that holds the agent definitions and is tracked.
/.claude/

*.log
.DS_Store
