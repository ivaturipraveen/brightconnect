# ============================================================
#  Chat UI
#  No secrets: the browser never sees the Anthropic key.
#  Requests go to /api and Vite proxies them to the chat API.
# ============================================================

# Only needed if the API is not on http://localhost:8080
# VITE_API_BASE=
